apiVersion: v1
kind: Service
metadata:
  name: propconnect-frontend
  namespace: propconnect
  labels:
    app.kubernetes.io/name: propconnect
    app.kubernetes.io/component: frontend
    app.kubernetes.io/part-of: propconnect
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: propconnect
    app.kubernetes.io/component: frontend
  ports:
    - name: http
      port: 80
      targetPort: http
